{
  "name": "@app/workflows-client",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "Shared workflow names, input schemas, and RPC types for the Cloudflare Workflows worker",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "clean": "rm -rf node_modules dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "fix": "eslint --fix --quiet ."
  },
  "dependencies": {
    "@ocavue/eslint-config": "^4.5.7",
    "eslint": "^10.4.0",
    "knip": "^6.14.1",
    "lint-staged": "^17.0.5",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@kyoto/eslint-config": "workspace:*",
    "@types/node": "catalog:",
    "tsconfig": "workspace:*",
    "typescript": "^6.0.3"
  }
}
